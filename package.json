{
  "name": "mock-api",
  "version": "1.0.0",
  "description": "Free mock API for Kishore's project",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
