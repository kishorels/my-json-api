{
  "name": "mock-api",
  "version": "1.0.0",
  "description": "Free mock API for Kishore's project",
  "main": "index.js",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port $PORT db.json"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
